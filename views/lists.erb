<ul id="lists">
  <% sort_lists(@lists) do |list, _| %>
    <li class=<%= list_class(list) %> >
      <a href="/lists/<%= list[:id] %>">
        <h2><%= list[:name] %></h2>
        <p><%= count_todos_remaining(list) %> / <%= count_todos(list) %></p>
      </a>
    </li>
  <% end %>
</ul>

<% content_for :header_links do %>
  <a class="add" href="/lists/new">New List</a>
<% end %>
